spring:
  datasource:
    url: jdbc:mysql://localhost:3306/catcate
    username: root
    password: 18697098685
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
  web:
    resources:
      static-locations: classpath:/static/,file:./src/main/resources/static/
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8081